# EduPulse - Цифровое сердце кафедры 🎓

Система мониторинга и анализа деятельности кафедры/института с продвинутой визуализацией и AI-предсказаниями.

## Концепция

EduPulse - это универсальный дашборд, который показывает "пульс" образовательного процесса в реальном времени. Система работает для трех целевых аудиторий:
- **Студенты**: анализ успеваемости, предсказание риска выгорания, геймификация
- **Преподаватели**: анализ эффективности преподавания, вовлеченность студентов
- **Администрация**: общая картина по кафедре, распределение ресурсов

## Технологический стек

- **Backend**: FastAPI (Python), PostgreSQL, SQLAlchemy
- **Frontend**: React + TypeScript + Tailwind CSS + Recharts
- **AI/ML**: scikit-learn для предсказаний
- **Деплой**: Docker + Nginx

## Структура проекта

```
uni_hackaton/
├── backend/          # FastAPI backend
├── frontend/         # React frontend
├── docker-compose.yml # Docker конфигурация
└── README.md
```

## Запуск

### Требования
- Docker и Docker Compose
- Или локально: Python 3.11+, Node.js 18+, PostgreSQL 15+

### Запуск через Docker (рекомендуется)

```bash
# Запуск всех сервисов
docker-compose up -d

# Просмотр логов
docker-compose logs -f

# Остановка
docker-compose down
```

После запуска:
- **Frontend**: http://localhost:3000
- **Backend API**: http://localhost:8000
- **API Docs**: http://localhost:8000/docs

### Локальный запуск

#### Backend:
```bash
cd backend
pip install -r requirements.txt
export DATABASE_URL=postgresql://edupulse:edupulse@localhost:5432/edupulse
python app/init_db.py  # Инициализация БД
uvicorn app.main:app --reload
```

#### Frontend:
```bash
cd frontend
npm install
npm start
```

## Особенности

- ✅ Генерация реалистичных синтетических данных
- ✅ AI-предсказания риска выгорания и успеваемости
- ✅ Геймификация с достижениями и рейтингами
- ✅ Три режима просмотра (студент/преподаватель/администрация)
- ✅ Интерактивные графики и визуализация
- ✅ Real-time статистика
- ✅ Адаптивный дизайн

## Структура API

Основные эндпоинты:
- `GET /api/dashboard/stats` - общая статистика
- `GET /api/students` - список студентов
- `GET /api/students/{id}` - детальная статистика студента
- `GET /api/courses` - список курсов
- `GET /api/leaderboard` - рейтинг студентов
- `GET /api/activity/timeline` - временная линия активности
- `POST /api/generate-data` - генерация данных (автоматически при запуске)

